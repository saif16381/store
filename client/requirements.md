## Packages
zustand | UI state management for auth modals and loading states
@hookform/resolvers | Form validation resolver for Zod
react-hook-form | Form state management
framer-motion | Animations and page transitions

## Notes
- Tailwind Config: Need to extend font family to include 'display' and 'sans'
- Backend assumes standard session/cookie based auth with /api/auth/* endpoints
- The `shared/routes` and `shared/schema` are assumed to be generated by the backend and importable
